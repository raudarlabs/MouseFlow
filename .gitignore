node_modules/
.vercel
.DS_Store
Thumbs.db
*.log

.vercel
.env*.local

# The React app's build output and its public copy of the agent: both are generated, and the agent has one
# source of truth at agent/mouseflow-agent.ps1 which the build copies in.
web/dist/
# Две сборки на один продукт - см. web/scripts/two-products.mjs.
web/dist-do/
web/dist-make/
web/public/agent/

# Built by web/vite.extension.config.ts — Chrome loads this, git does not carry it.
extension/dist/

# Дампы базы. Скрипт бэкапа умеет оставлять файл локально (--local), а открытый дамп живой базы в рабочей
# папке - это утечка, ожидающая коммита.
*.dump
*.dump.age
backup-key.txt

# Артефакт `tsc -b` - кэш инкрементальной сборки, а не исходник.
tsconfig.tsbuildinfo
